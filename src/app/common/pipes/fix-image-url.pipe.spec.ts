import { FixImageUrlPipe } from './fix-image-url.pipe';

describe('FixImageUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new FixImageUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
